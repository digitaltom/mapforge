#map.map

#flash
  #flash-container

= render 'feature_details'

.banner-overlay{'data-controller': "feature"}

= render 'map_modal'
= render 'share_modal'
= render 'feature_popup'

#scaleline-metric.scaleline-external
